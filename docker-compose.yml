version: "3.8"
services:
  django_user:
    build:
      context: ./user_service
    ports:
      - "8000:8000"
  flask_books:
    build:
      context: ./book_service
    ports:
      - "5000:5000"
  fastapi_recommend:
    build:
      context: ./recommendation_service
    ports:
      - "8001:8001"
